// const path = require('path');

module.exports = {
  baseUrl: "/",
  // outputDir: "server/public",
  lintOnSave: true,
  runtimeCompiler: false,
  configureWebpack: {
    devtool: "source-map"
  }
};
